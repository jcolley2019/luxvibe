## Packages
date-fns | Date formatting and manipulation
react-day-picker | Date picker component for booking dates
framer-motion | Animations for page transitions and UI elements
lucide-react | Icons (already in base, but confirming usage)
clsx | Class name utility (already in base)
tailwind-merge | Class name merging (already in base)

## Notes
- Using Replit Auth for user authentication (useAuth hook).
- Landing page needs to be high quality with hero section.
- Search functionality uses query params.
- Booking flow requires date selection.
